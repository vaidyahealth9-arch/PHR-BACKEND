# PHR Backend - Local Setup

This directory contains the necessary configuration to run the PHR Backend service independently.

## Getting Started

To run this service locally using Docker Compose, navigate to this directory and execute the following command:

```bash
docker-compose up --build
```

### Details
- **Port**: 8000
- **Dependencies**: Ensure the PHR database is running if required by the application.
